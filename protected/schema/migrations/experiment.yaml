---
table: experiment 
from: null
to: stpn_1
depends:
  - [dataset, bgh_1]
sql: |
  CREATE TABLE experiment (
    id SERIAL NOT NULL PRIMARY KEY,
    exp_description VARCHAR(1000) NULL,
    experiment_name VARCHAR(100) NULL,
    experiment_type VARCHAR(100) NULL,
    dataset_id INTEGER NULL REFERENCES dataset(id)
  );